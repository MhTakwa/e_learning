{{ form_start(form) }}
   <div class="form-group">
     
        {{ form_label(form.label) }}
    
    <div class="form-line">
        {{ form_widget(form.label,{ 'attr': { 'class': 'form-control' } } ) }}
        {{ form_errors(form.label) }}
    </div>

 </div>
 </div>

   <div class="col-md-12">
    <div class="col-md-6">
     <a class="back btn btn-danger m-t-15 waves-effect" href="{{ path('category_index') }}">back to list</a>
     </div>
     <div class="col-md-6">
    <button class="submit btn btn-primary m-t-15 waves-effect">{{ button_label|default('Save') }}</button>
    </div>
</div>
{{ form_end(form) }}
